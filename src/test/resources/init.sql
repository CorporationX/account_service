CREATE TABLE IF NOT EXISTS account (
    id BIGSERIAL PRIMARY KEY,
    number VARCHAR(20) NOT NULL UNIQUE,
    type VARCHAR(50) NOT NULL,
    currency VARCHAR(3) NOT NULL,
    status VARCHAR(20) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL,
    closed_at TIMESTAMP NULL,
    version INT DEFAULT 1 NOT NULL
);

CREATE INDEX idx_account_number ON account(number);

INSERT INTO account (number, type, currency, status, created_at, updated_at, closed_at, version) VALUES
('ACC001', 'CORPORATEACCOUNT', 'USD', 'ACTIVE', CURRENT_TIMESTAMP, CURRENT_TIMESTAMP, NULL, 1),
('ACC002', 'CREDITACCOUNT', 'EUR', 'ACTIVE', CURRENT_TIMESTAMP, CURRENT_TIMESTAMP, NULL, 1),
('ACC003', 'TRUSTACCOUNT', 'EUR', 'ACTIVE', CURRENT_TIMESTAMP, CURRENT_TIMESTAMP, NULL, 1),
('ACC004', 'CORPORATEACCOUNT', 'USD', 'BLOCK', CURRENT_TIMESTAMP, CURRENT_TIMESTAMP, NULL, 1),
('ACC005', 'INVESTMENTACCOUNT', 'EUR', 'CLOSED', CURRENT_TIMESTAMP, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP, 1);