spring:
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:postgresql:13.3://localhost/testdb
    username: testuser
    password: testpassword

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false