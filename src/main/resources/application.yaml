spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres
    username: user
    password: password

account:
  numbers:
    generation:
      cron-for-type-individual: "0 0 2 * * ?"
      cron-for-type-savings: "0 0 0 * * ?"
      cron-for-type-investment: "0 0 3 * * ?"
      # ...
    batch-size:
      checking-individual: 1000
      checking-corporate: 500
      currency_account: 300
      savings_account: 400
      investment_account: 100

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  data:
    redis:
      port: 6379
      host: localhost
      channels:
        init_payment: init_payment_channel
        progress_payment: progress_payment_channel
        result_progress_payment: result_progress_payment_channel

  kafka:
    bootstrap-servers: "localhost:9092"
    pool-size: 3
    consumer:
      group-id: "group"
      auto-offset-reset: earliest
      enable-auto-commit: false
    topics:
      payment-authorization:
        name: payment-authorization-topic
        num-partitions: 5
        replication-factor: 2
      payment-clear:
        name: payment-clear-topic
        num-partitions: 3
        replication-factor: 1
      payment-status-reset:
        name: payment-status-reset-topic
        num-partitions: 3
        replication-factor: 1

  data:
    redis:
      port: 6379
      host: localhost
      channels:
        request_status_notification: request_status_notification_channel

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml

server:
  port: 8090

logging:
  level:
    root: info
