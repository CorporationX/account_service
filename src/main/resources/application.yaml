spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres
    username: user
    password: password

account:
  numbers:
    generation:
      cron-for-type-individual: "0 0 2 * * ?"
      cron-for-type-savings: "0 0 0 * * ?"
      cron-for-type-investment: "0 0 3 * * ?"
      # ...
    batch-size:
      checking-individual: 1000
      checking-corporate: 500
      currency_account: 300
      savings_account: 400
      investment_account: 100

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  data:
    redis:
      port: 6379
      host: localhost
      channels:
        request_status_notification: request_status_notification_channel

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml

server:
  port: 8090

logging:
  level:
    root: info

interest:
  accrual:
    days: 365
    hours: 24

scheduled:
  task:
    cronForAccrual: "0 0 0 * * *"
    cronForGenerateNumbers: "0 0 * * * *"
    amountGenerateNumbers: 100

poolThreads:
  poolForAccrual: 10