name: CI

on:
  pull_request:
    branches:
      - 'dev'
  push:
    branches:
      - '*'
jobs:
  build:
    runs-on: ubuntu-18.04
    steps:
       - run:
           ./gradlew clean build -x test
         with:
          java-version: 17